# DDJ-FLX4 Reader 使用文档

这个项目用于在 Windows 上读取 Pioneer DDJ-FLX4 的 MIDI 输入，并且可以通过 MIDI OUT 控制 FLX4 的灯光。

## 1. 准备工作

硬件：

```text
Windows 电脑
Pioneer DDJ-FLX4
USB Type-C 数据线
```

软件：

```text
Python 3.x
mido
python-rtmidi
```

安装依赖：

```powershell
cd C:\Users\17764\Desktop\DJ\FLX4work
pip install -r requirements.txt
```

使用前建议关闭 rekordbox / Serato 等会占用 FLX4 MIDI 端口的软件。

## 2. 查看设备

先确认系统能看到 FLX4：

```powershell
python main.py --list-devices
```

正常会看到 Input devices 和 Output devices 两部分。如果自动识别失败，后面可以用 `--device` 或 `--out-device` 手动指定完整设备名。

## 3. 读取 FLX4 控制事件

只读取按钮、旋钮、推子、转盘事件：

```powershell
python main.py
```

输出格式：

```text
B 编号 状态
C 编号 百分比
J 编号 方向
```

示例：

```text
B 1 1
B 1 0
C 3 50.393701
J 1 1
J 1 -1
```

含义：

```text
B = Button，按钮，1 是按下，0 是松开
C = Control，旋钮/推子，数值是 0.000000 到 100.000000
J = Jog，转盘，1 是一个方向，-1 是另一个方向
```

调试未映射 MIDI 消息：

```powershell
python main.py --debug
```

## 4. 自动跑灯

让程序自动挨个点亮、熄灭支持的灯：

```powershell
python main.py --light-demo
```

默认逻辑：

```text
1. L1 到 L22 逐个亮一下再灭
2. L23 到 L38，也就是 16 个打击垫灯，逐个亮一下再灭
3. L41 到 L42，也就是 Vinyl 灯，逐个亮一下再灭
4. M1/M2 电平灯从 1 扫到 5，然后归 0
```

调整速度：

```powershell
python main.py --light-demo --light-demo-delay 0.08
```

一直循环，按 `Ctrl+C` 停止：

```powershell
python main.py --light-demo --light-demo-repeat 0
```

循环 3 次：

```powershell
python main.py --light-demo --light-demo-repeat 3
```

指定 MIDI OUT 设备：

```powershell
python main.py --light-demo --out-device "你的 FLX4 输出设备名"
```

## 5. 手动发送一条灯光命令

点亮某个灯：

```powershell
python main.py --light-command "L1 1"
```

关闭某个灯：

```powershell
python main.py --light-command "L1 0"
```

也可以一次发送多条：

```powershell
python main.py --light-command "L1 1" --light-command "L2 1" --light-command "M1 5"
```

## 6. 灯光命令格式

普通灯：

```text
L 灯编号 状态
```

状态：

```text
1 = 亮
0 = 灭
```

也支持紧凑写法：

```text
L1 1
L23 0
```

电平灯：

```text
M 电平编号 等级
```

等级范围：

```text
0 到 5
```

示例：

```powershell
python main.py --light-command "M1 5"
python main.py --light-command "M2 3"
python main.py --light-command "M1 0"
```

Pad 模式：

```text
P Deck编号 模式名
```

示例：

```powershell
python main.py --light-command "P1 SAMPLER" --light-command "L23 1"
```

含义：把 Deck1 的 Pad 灯地址模式设为 `SAMPLER`，然后点亮 Deck1 Pad1。

支持的 Pad 模式：

```text
HOT_CUE
PAD_FX1
BEAT_JUMP
SAMPLER
KEYBOARD
PAD_FX2
BEAT_LOOP
KEY_SHIFT
```

## 7. 常用灯号

核心按钮灯：

```text
L1   Deck1 PLAY/PAUSE
L2   Deck1 CUE
L3   Deck2 PLAY/PAUSE
L4   Deck2 CUE
L5   Deck1 IN
L6   Deck1 OUT
L7   Deck2 IN
L8   Deck2 OUT
L9   Deck1 BEAT SYNC
L10  Deck2 BEAT SYNC
L11  CH1 CUE
L12  CH2 CUE
L13  SMART CFX
L14  SMART FADER
```

Pad 模式灯：

```text
L15  Deck1 HOT CUE
L16  Deck1 PAD FX1
L17  Deck1 BEAT JUMP
L18  Deck1 SAMPLER
L19  Deck2 HOT CUE
L20  Deck2 PAD FX1
L21  Deck2 BEAT JUMP
L22  Deck2 SAMPLER
```

打击垫灯：

```text
L23  Deck1 Pad1
L24  Deck1 Pad2
L25  Deck1 Pad3
L26  Deck1 Pad4
L27  Deck1 Pad5
L28  Deck1 Pad6
L29  Deck1 Pad7
L30  Deck1 Pad8

L31  Deck2 Pad1
L32  Deck2 Pad2
L33  Deck2 Pad3
L34  Deck2 Pad4
L35  Deck2 Pad5
L36  Deck2 Pad6
L37  Deck2 Pad7
L38  Deck2 Pad8
```

其他：

```text
L41  Deck1 Vinyl Mode
L42  Deck2 Vinyl Mode
M1   CH1 Level Meter，0 到 5
M2   CH2 Level Meter，0 到 5
```

## 8. 边读取事件边控制灯

运行：

```powershell
python main.py --lights
```

这个模式会继续输出 FLX4 的 `B/C/J` 控制事件，同时从终端输入灯光命令。

示例输入：

```text
L1 1
L1 0
P1 SAMPLER
L23 1
M1 5
```

如果上层程序要接入，可以读取本程序的 stdout 获得控制事件，同时向本程序的 stdin 写入灯光命令。

## 9. 在 Python 代码里当库使用

灯光控制已经封装成库，可以直接 import。

示例：点亮 Deck1 Pad1：

```python
from flx4_reader.light_control import LightController
from flx4_reader.midi_device import open_output_device

with open_output_device() as out:
    lights = LightController(out)
    lights.send_light(23, 1)
```

关闭 Deck1 Pad1：

```python
from flx4_reader.light_control import LightController
from flx4_reader.midi_device import open_output_device

with open_output_device() as out:
    lights = LightController(out)
    lights.send_light(23, 0)
```

设置 Pad 模式后点亮：

```python
from flx4_reader.light_control import LightController
from flx4_reader.midi_device import open_output_device

with open_output_device() as out:
    lights = LightController(out)
    lights.handle_line("P1 SAMPLER")
    lights.handle_line("L23 1")
```

控制电平灯：

```python
from flx4_reader.light_control import LightController
from flx4_reader.midi_device import open_output_device

with open_output_device() as out:
    lights = LightController(out)
    lights.send_level_meter(1, 5)
    lights.send_level_meter(2, 3)
```

自动跑灯：

```python
from flx4_reader.light_control import LightController, run_light_demo
from flx4_reader.midi_device import open_output_device

with open_output_device() as out:
    lights = LightController(out)
    run_light_demo(lights, delay=0.08, repeats=1)
```

## 10. 函数说明

常用库函数和类：

```python
LightController(output)
```

灯光控制器，需要传入一个 mido MIDI output。

```python
lights.send_light(light_id, state)
```

发送普通灯命令。`state` 为 `1` 表示亮，`0` 表示灭。

```python
lights.send_level_meter(meter_id, level)
```

发送电平灯命令。`meter_id` 为 `1` 或 `2`，`level` 为 `0` 到 `5`。

```python
lights.handle_line(command)
```

解析并执行文本命令，比如 `L23 1`、`M1 5`、`P1 SAMPLER`。

```python
run_light_demo(lights, delay=0.12, repeats=1)
```

自动跑灯。`repeats=0` 表示无限循环。

设备相关：

```python
open_input_device()
open_output_device()
list_input_devices()
list_output_devices()
```

## 11. 亮度说明

普通按钮灯目前按 FLX4 MIDI 表实现为开关：

```text
0x00 = 灭
0x7F = 亮
```

也就是说普通灯不是连续亮度控制。

电平灯支持等级：

```text
M1/M2 的等级为 0 到 5
```

这表示亮几格，不是单个灯的亮度。

## 12. 常见问题

### 找不到 FLX4

先运行：

```powershell
python main.py --list-devices
```

确认系统能看到 FLX4。检查 USB 线、设备是否通电，以及是否被其他 DJ 软件占用。

### MIDI 端口打不开

通常是 rekordbox / Serato / 其他 MIDI 软件正在占用设备。关闭它们后重新运行。

### 灯不亮

先试：

```powershell
python main.py --light-demo
```

如果自动识别不到输出设备，用：

```powershell
python main.py --list-devices
python main.py --light-demo --out-device "你的 FLX4 输出设备名"
```

### Pad 灯点错位置

Pad 灯地址和模式有关。先指定模式再点灯：

```powershell
python main.py --light-command "P1 HOT_CUE" --light-command "L23 1"
python main.py --light-command "P1 SAMPLER" --light-command "L23 1"
```

### 只想测试一个灯

用：

```powershell
python main.py --light-command "L23 1"
python main.py --light-command "L23 0"
```

## 13. 推荐测试顺序

第一次使用建议按这个顺序：

```powershell
cd C:\Users\17764\Desktop\DJ\FLX4work
pip install -r requirements.txt
python main.py --list-devices
python main.py --light-demo
python main.py --light-command "L23 1"
python main.py --light-command "L23 0"
python main.py
```

## 14. 按键和灯光编号速查

这一节按人能读懂的方式列出编号。程序读取 FLX4 时输出 `B 编号 状态`，控制灯光时接收 `L 编号 状态` 或 `M 编号 等级`。

### 14.1 基础按键输入

| 输出编号 | 控件 |
| --- | --- |
| B1 | Deck1 PLAY/PAUSE |
| B2 | Deck1 PLAY/PAUSE + SHIFT |
| B3 | Deck1 CUE |
| B4 | Deck1 CUE + SHIFT |
| B5 | Deck1 SHIFT |
| B6 | Deck1 Jog 顶面触摸 |
| B7 | Deck1 Jog 顶面触摸 + SHIFT |
| B8 | Deck1 IN |
| B9 | Deck1 IN + SHIFT |
| B10 | Deck1 OUT |
| B11 | Deck1 OUT + SHIFT |
| B12 | Deck2 PLAY/PAUSE |
| B13 | Deck2 PLAY/PAUSE + SHIFT |
| B14 | Deck2 CUE |
| B15 | Deck2 CUE + SHIFT |
| B16 | Deck2 SHIFT |
| B17 | Deck2 Jog 顶面触摸 |
| B18 | Deck2 Jog 顶面触摸 + SHIFT |
| B19 | Deck2 IN |
| B20 | Deck2 IN + SHIFT |
| B21 | Deck2 OUT |
| B22 | Deck2 OUT + SHIFT |

### 14.2 Loop / Sync 按键输入

| 输出编号 | 控件 |
| --- | --- |
| B23 | Deck1 4 BEAT / EXIT |
| B24 | Deck1 4 BEAT / EXIT + SHIFT |
| B25 | Deck1 CUE/LOOP CALL 左 |
| B26 | Deck1 CUE/LOOP CALL 左 + SHIFT |
| B27 | Deck1 CUE/LOOP CALL 右 |
| B28 | Deck1 CUE/LOOP CALL 右 + SHIFT |
| B29 | Deck1 BEAT SYNC |
| B30 | Deck1 BEAT SYNC 长按 |
| B31 | Deck1 BEAT SYNC + SHIFT |
| B32 | Deck2 4 BEAT / EXIT |
| B33 | Deck2 4 BEAT / EXIT + SHIFT |
| B34 | Deck2 CUE/LOOP CALL 左 |
| B35 | Deck2 CUE/LOOP CALL 左 + SHIFT |
| B36 | Deck2 CUE/LOOP CALL 右 |
| B37 | Deck2 CUE/LOOP CALL 右 + SHIFT |
| B38 | Deck2 BEAT SYNC |
| B39 | Deck2 BEAT SYNC 长按 |
| B40 | Deck2 BEAT SYNC + SHIFT |

### 14.3 FX / Mixer / Browse 输入

| 输出编号 | 控件 |
| --- | --- |
| B41-B44 | FX CH SELECT 的状态按键消息 |
| B45 | FX SELECT |
| B46 | FX SELECT + SHIFT |
| B47 | FX BEAT 左 |
| B48 | FX BEAT 左 + SHIFT |
| B49 | FX BEAT 右 |
| B50 | FX BEAT 右 + SHIFT |
| B51 | FX ON/OFF CH1 |
| B52 | FX ON/OFF CH2 |
| B53 | FX ON/OFF CH1 + SHIFT |
| B54 | FX ON/OFF CH2 + SHIFT |
| B55 | MASTER CUE |
| B56 | MASTER CUE + SHIFT |
| B57 | CH1 CUE |
| B58 | CH1 CUE + SHIFT |
| B59 | CH1 Fader Start PLAY |
| B60 | CH1 Fader Start CUE |
| B61 | CH2 CUE |
| B62 | CH2 CUE + SHIFT |
| B63 | CH2 Fader Start PLAY |
| B64 | CH2 Fader Start CUE |
| B65 | SMART CFX |
| B66 | SMART CFX + SHIFT |
| B67 | SMART FADER |
| B68 | SMART FADER + SHIFT |
| B69 | Android MONO/STEREO |
| B70 | BROWSE 按下 |
| B71 | BROWSE 按下 + SHIFT |
| B72 | LOAD Deck1 |
| B73 | LOAD Deck1 + SHIFT |
| B74 | LOAD Deck2 |
| B75 | LOAD Deck2 + SHIFT |

### 14.4 Pad 模式按键输入

| 输出编号 | 控件 |
| --- | --- |
| B76 | Deck1 HOT CUE 模式 |
| B77 | Deck1 KEYBOARD 模式 |
| B78 | Deck1 PAD FX1 模式 |
| B79 | Deck1 PAD FX2 模式 |
| B80 | Deck1 BEAT JUMP 模式 |
| B81 | Deck1 BEAT LOOP 模式 |
| B82 | Deck1 SAMPLER 模式 |
| B83 | Deck1 KEY SHIFT 模式 |
| B84 | Deck2 HOT CUE 模式 |
| B85 | Deck2 KEYBOARD 模式 |
| B86 | Deck2 PAD FX1 模式 |
| B87 | Deck2 PAD FX2 模式 |
| B88 | Deck2 BEAT JUMP 模式 |
| B89 | Deck2 BEAT LOOP 模式 |
| B90 | Deck2 SAMPLER 模式 |
| B91 | Deck2 KEY SHIFT 模式 |

### 14.5 打击垫输入编号怎么看

打击垫输入编号很多，范围是 `B92` 到 `B347`。它们按 Pad 1 到 Pad 8 分组，每个 Pad 占 32 个编号。

每个 Pad 内部的模式顺序是：

```text
HOT_CUE, PAD_FX1, BEAT_JUMP, SAMPLER, KEYBOARD, PAD_FX2, BEAT_LOOP, KEY_SHIFT
```

以 Pad 1 为例：

| 输出编号 | 控件 |
| --- | --- |
| B92 | Deck1 Pad1 HOT_CUE |
| B93 | Deck1 Pad1 HOT_CUE + SHIFT |
| B94 | Deck1 Pad1 PAD_FX1 |
| B95 | Deck1 Pad1 PAD_FX1 + SHIFT |
| B96 | Deck1 Pad1 BEAT_JUMP |
| B97 | Deck1 Pad1 BEAT_JUMP + SHIFT |
| B98 | Deck1 Pad1 SAMPLER |
| B99 | Deck1 Pad1 SAMPLER + SHIFT |
| B100 | Deck1 Pad1 KEYBOARD |
| B101 | Deck1 Pad1 KEYBOARD + SHIFT |
| B102 | Deck1 Pad1 PAD_FX2 |
| B103 | Deck1 Pad1 PAD_FX2 + SHIFT |
| B104 | Deck1 Pad1 BEAT_LOOP |
| B105 | Deck1 Pad1 BEAT_LOOP + SHIFT |
| B106 | Deck1 Pad1 KEY_SHIFT |
| B107 | Deck1 Pad1 KEY_SHIFT + SHIFT |
| B108 | Deck2 Pad1 HOT_CUE |
| B109 | Deck2 Pad1 HOT_CUE + SHIFT |
| B110 | Deck2 Pad1 PAD_FX1 |
| B111 | Deck2 Pad1 PAD_FX1 + SHIFT |
| B112 | Deck2 Pad1 BEAT_JUMP |
| B113 | Deck2 Pad1 BEAT_JUMP + SHIFT |
| B114 | Deck2 Pad1 SAMPLER |
| B115 | Deck2 Pad1 SAMPLER + SHIFT |
| B116 | Deck2 Pad1 KEYBOARD |
| B117 | Deck2 Pad1 KEYBOARD + SHIFT |
| B118 | Deck2 Pad1 PAD_FX2 |
| B119 | Deck2 Pad1 PAD_FX2 + SHIFT |
| B120 | Deck2 Pad1 BEAT_LOOP |
| B121 | Deck2 Pad1 BEAT_LOOP + SHIFT |
| B122 | Deck2 Pad1 KEY_SHIFT |
| B123 | Deck2 Pad1 KEY_SHIFT + SHIFT |

Pad 2 在 Pad 1 的基础上每个编号加 32，Pad 3 加 64，依此类推。比如：

| 输出编号 | 控件 |
| --- | --- |
| B124 | Deck1 Pad2 HOT_CUE |
| B156 | Deck1 Pad3 HOT_CUE |
| B188 | Deck1 Pad4 HOT_CUE |
| B220 | Deck1 Pad5 HOT_CUE |
| B252 | Deck1 Pad6 HOT_CUE |
| B284 | Deck1 Pad7 HOT_CUE |
| B316 | Deck1 Pad8 HOT_CUE |
| B332 | Deck2 Pad8 HOT_CUE |
| B346 | Deck2 Pad8 KEY_SHIFT |

### 14.6 灯光编号

控制普通灯时用：

```text
L编号 1 = 亮
L编号 0 = 灭
```

| 命令编号 | 灯 |
| --- | --- |
| L1 | Deck1 PLAY/PAUSE |
| L2 | Deck1 CUE |
| L3 | Deck2 PLAY/PAUSE |
| L4 | Deck2 CUE |
| L5 | Deck1 IN |
| L6 | Deck1 OUT |
| L7 | Deck2 IN |
| L8 | Deck2 OUT |
| L9 | Deck1 BEAT SYNC |
| L10 | Deck2 BEAT SYNC |
| L11 | CH1 CUE |
| L12 | CH2 CUE |
| L13 | SMART CFX |
| L14 | SMART FADER |
| L15 | Deck1 HOT CUE 模式灯 |
| L16 | Deck1 PAD FX1 模式灯 |
| L17 | Deck1 BEAT JUMP 模式灯 |
| L18 | Deck1 SAMPLER 模式灯 |
| L19 | Deck2 HOT CUE 模式灯 |
| L20 | Deck2 PAD FX1 模式灯 |
| L21 | Deck2 BEAT JUMP 模式灯 |
| L22 | Deck2 SAMPLER 模式灯 |
| L23-L30 | Deck1 Pad1 到 Pad8 |
| L31-L38 | Deck2 Pad1 到 Pad8 |
| L41 | Deck1 Vinyl Mode |
| L42 | Deck2 Vinyl Mode |

电平灯用 `M` 命令：

| 命令编号 | 灯 | 等级 |
| --- | --- | --- |
| M1 | CH1 Level Meter | 0 到 5 |
| M2 | CH2 Level Meter | 0 到 5 |

### 14.7 常用灯光例子

| 目的 | 命令 |
| --- | --- |
| 点亮 Deck1 PLAY | `python main.py --light-command "L1 1"` |
| 熄灭 Deck1 PLAY | `python main.py --light-command "L1 0"` |
| 点亮 Deck1 Pad1 | `python main.py --light-command "L23 1"` |
| 熄灭 Deck1 Pad1 | `python main.py --light-command "L23 0"` |
| 点亮 Deck2 Pad8 | `python main.py --light-command "L38 1"` |
| CH1 电平灯满格 | `python main.py --light-command "M1 5"` |
| CH2 电平灯亮 3 格 | `python main.py --light-command "M2 3"` |
| Deck1 Pad1 按 SAMPLER 模式点亮 | `python main.py --light-command "P1 SAMPLER" --light-command "L23 1"` |
